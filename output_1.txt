Example Output

Below is a screenshot showing the usage and output of the PowerShell script:

![Service_check.ps1 Example Output](./Service_check_ps1_example.png)

<Filepath> cat .\Service_check.ps1
# Ask for service/command name
$inputName = Read-Host "Enter the name of the service or program to check"

# 1. Check if it's a Windows Service
$service = Get-Service -Name $inputName -ErrorAction SilentlyContinue

if ($service) {
    Write-Output "âœ… $inputName is installed as a Windows Service."

    if ($service.Status -eq "Running") {
        Write-Output "âœ… $inputName service is running."
    } else {
        Write-Output "âŒ $inputName service is installed but NOT running. Current status: $($service.Status)"
    }
}
else {
    # 2. Check if it's an executable in PATH
    $program = Get-Command $inputName -ErrorAction SilentlyContinue

    if ($program) {
        Write-Output "âœ… $inputName is installed and available in PATH."
    } else {
        Write-Output "âŒ $inputName is NOT installed."
    }
}
# 3. Check if it's a package installed via winget
$wingetList = winget list | Select-String -Pattern $inputName
if ($wingetList) {
    Write-Output "âœ… $inputName is installed via winget."
} else {
    Write-Output "âŒ $inputName is NOT installed via winget."
}

<Filepatht> .\Service_check.ps1
Enter the name of the service or program to check: python
âœ… python is installed and available in PATH.
âœ… python is installed via winget.


